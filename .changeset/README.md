# Changesets

This folder holds the pending changesets for `starlight-shadcn`. Add one with
`pnpm changeset` in any PR that changes the published package — CI enforces it.

See [the changesets docs](https://github.com/changesets/changesets) for the full
format.
