<div class="lg:sl-flex shadcn-two-column">
	{
		Astro.locals.starlightRoute.toc && (
			<aside class="right-sidebar-container print:hidden">
				<div class="right-sidebar">
					<slot name="right-sidebar" />
				</div>
			</aside>
		)
	}
	<div class="main-pane"><slot /></div>
</div>

<style>
	@layer starlight-shadcn {
		.main-pane {
			isolation: isolate;
		}

		@media (min-width: 72rem) {
			.right-sidebar-container {
				order: 2;
				position: relative;
				width: max(
					var(--sl-sidebar-width),
					calc(
						var(--sl-sidebar-width) + (100% - var(--sl-content-width) - var(--sl-sidebar-col)) / 2
					)
				);
			}

			.right-sidebar {
				position: fixed;
				top: var(--sl-docs-row-1);
				border-inline-start: 1px solid var(--border);
				padding-top: calc(var(--sl-docs-row-2) - var(--sl-docs-row-1));
				width: 100%;
				height: calc(100vh - var(--sl-docs-row-1));
				overflow-y: auto;
				scrollbar-width: none;
			}

			.main-pane {
				width: 100%;
			}

			:global([data-has-sidebar][data-has-toc]) .main-pane {
				--sl-content-margin-inline: auto 0;
				order: 1;
				width: min(
					calc(100% - var(--sl-sidebar-col)),
					calc(
						var(--sl-content-width) + (100% - var(--sl-content-width) - var(--sl-sidebar-col)) / 2
					)
				);
			}
		}
	}
</style>
