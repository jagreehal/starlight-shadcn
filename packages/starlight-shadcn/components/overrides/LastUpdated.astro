---
// Pass-through: upstream ships no styles for this at all, so the fork existed
// only to attach a class. `styles/components.css` targets it through the footer
// meta selectors instead.
import Default from '@astrojs/starlight/components/LastUpdated.astro';
---

<Default />
