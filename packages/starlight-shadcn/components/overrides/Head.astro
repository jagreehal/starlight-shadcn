---
// Pass-through: nothing about `<head>` is themeable. The override is registered
// so the set stays complete, but the implementation stays Starlight's.
import Default from '@astrojs/starlight/components/Head.astro';
---

<Default />
