---
// Pass-through: markup is identical to upstream and its border already resolves
// through the token bridge. The two spacing tweaks live in
// `styles/components.css`.
import Default from '@astrojs/starlight/components/MobileMenuFooter.astro';
---

<Default />
