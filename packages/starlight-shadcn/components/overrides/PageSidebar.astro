---
// Pass-through: upstream's layout and type scale are already what we want, and
// its `--sl-color-gray-3` / `--sl-color-white` link colours resolve through the
// token bridge. The only additions — hover padding and radius on TOC links —
// live in `styles/components.css`.
import Default from '@astrojs/starlight/components/PageSidebar.astro';
---

<Default />
