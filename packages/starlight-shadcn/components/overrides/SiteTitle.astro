---
// Pass-through: upstream's layout, type scale and logo sizing are already what
// we want. Only the link colour differs, and that lives in
// `styles/components.css`.
import Default from '@astrojs/starlight/components/SiteTitle.astro';
---

<Default />
