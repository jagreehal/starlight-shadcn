---
// Pass-through: the banner needs no markup changes, only colour, which
// `styles/components.css` applies to `.sl-banner`. Forking the template here
// would duplicate Starlight's logic with nothing to show for it.
import Default from '@astrojs/starlight/components/Banner.astro';
---

<Default />
